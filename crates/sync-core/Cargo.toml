[package]
name = "sync-core"
version = "0.1.0"
edition = "2021"
description = "WASM sync primitives for SyncPlayer."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
js-sys = "0.3.99"
mp4 = "0.14.0"
wasm-bindgen = "0.2"

